# Программа, демонстрирующая закон сохранения импульса
m1 = float(input("Введите массу первого тела (в килограммах): "))
v1 = float(input("Введите скорость первого тела (в метрах в секунду): "))
m2 = float(input("Введите массу второго тела (в килограммах): "))
v2 = float(input("Введите скорость второго тела (в метрах в секунду): "))

p1 = m1 * v1  # импульс первого тела
p2 = m2 * v2  # импульс второго тела

p_total_before = p1 + p2  # общий импульс до столкновения

v1_new = (p_total_before - p2) / m1  # новая скорость первого тела после столкновения
v2_new = (p_total_before - p1) / m2  # новая скорость второго тела после столкновения

p1_new = m1 * v1_new  # импульс первого тела после столкновения
p2_new = m2 * v2_new  # импульс второго тела после столкновения

print("Суммарный импульс до столкновения: {:.2f} кг·м/с".format(p_total_before))
print("Новая скорость первого тела: {:.2f} м/с".format(v1_new))
print("Новая скорость второго тела: {:.2f} м/с".format(v2_new))
print("Суммарный импульс после столкновения: {:.2f} кг·м
